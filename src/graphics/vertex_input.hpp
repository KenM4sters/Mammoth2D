

/**
 * @brief Name is TBC given the clash with VkVertexInput but for now I think it is a fitting 
 * name since it will hold all information about vertex data (including the data itself).
 */
class IVertexInput 
{ 
    IVertexInput() {}
    virtual ~IVertexInput() {}
};